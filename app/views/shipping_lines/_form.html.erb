<%= form_with(model: shipping_line, local: true) do |form| %>
  <% if shipping_line.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping_line.errors.count, "error") %> prohibited this shipping_line from being saved:</h2>

      <ul>
      <% shipping_line.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :shipping_line_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
